run:
  timeout: 5m
linters:
  enable:
    - govet
    - gofmt
    - gofumpt
    - goimports
    - errcheck
    - staticcheck
issues:
  exclude-dirs:
    - web/admin
    - testdata
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
