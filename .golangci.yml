run:
  timeout: 5m
  modules-download-mode: mod
linters:
  enable:
    - govet
    - gofmt
    - gofumpt
    - goimports
    - errcheck
    - staticcheck
issues:
  exclude-dirs:
    - web/admin
    - testdata
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
